<?xml version="1.0"?>
<package format="3">
  <name>ros_gz_tools</name>
  <version>1.0.0</version>
  <description>Gazebo Sim resources and reusable launch helpers for simulation bringup.</description>
  <maintainer email="jfrascon@gmail.com">Juan Francisco Rascon Crespo</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>ament_cmake_python</buildtool_depend>

  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>
  <exec_depend>ros2_launch_helpers</exec_depend>
  <exec_depend>ros_gz_bridge</exec_depend>
  <exec_depend>ros_gz_sim</exec_depend>

  <test_depend>ament_cmake_flake8</test_depend>
  <test_depend>ament_cmake_lint_cmake</test_depend>
  <test_depend>ament_cmake_pep257</test_depend>
  <test_depend>ament_cmake_pytest</test_depend>
  <test_depend>ament_cmake_xmllint</test_depend>
  <test_depend>ament_index_python</test_depend>
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
    <!--
    ros_gz_sim reads these gazebo_ros exports and appends them to GZ_SIM_RESOURCE_PATH before the
    Gazebo server starts.

    This package uses both URI patterns inside SDF files:
    - package://ros_gz_tools/... for worlds and top-level model files
    - model://&lt;model_name&gt;/... for model-local mesh references

    Gazebo therefore needs two search roots:
    - ${prefix}/.. lets package://ros_gz_tools/... resolve from share/
    - ${prefix}/models lets model://&lt;model_name&gt;/... resolve from the models directory

    Reference:
    https://github.com/gazebosim/ros_gz/tree/ros2/ros_gz_sim#using-gazebo_ros-to-export-model-paths-in-packagexml
    -->
    <gazebo_ros gazebo_model_path="${prefix}/.."/>
    <gazebo_ros gazebo_model_path="${prefix}/models"/>
  </export>
</package>
